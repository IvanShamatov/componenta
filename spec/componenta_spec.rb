require 'spec_helper'

describe Componenta do
  it 'has a version number' do
    expect(Componenta::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
